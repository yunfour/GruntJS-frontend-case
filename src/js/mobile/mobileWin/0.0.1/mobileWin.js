define(function(require) {
    
    var createClass = require('base/createClass/1.0.2/createClass'),
        pubSub      = require('base/pubSub/1.0.2/pubSub');

    var $ = require('zepto');

    var MobiWin = createClass({
        
    });

});